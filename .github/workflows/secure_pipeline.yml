name: Secure Pipeline

on:
  pull_request:
    branches:
      - test
      - main

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:

  security-check:

    runs-on: ubuntu-latest

    env:
      TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt
          pip install requests pytest

      - name: Telegram Inicio
        run: |
          python - <<EOF
          import requests, os

          requests.post(
              f"https://api.telegram.org/bot{os.environ['TELEGRAM_TOKEN']}/sendMessage",
              json={
                  "chat_id": os.environ["TELEGRAM_CHAT_ID"],
                  "text": "🔍 Inicio revisión de seguridad"
              }
          )
          EOF

      - name: Obtener archivos modificados 
        run: | 
          git diff --name-only origin/${{ github.base_ref }} HEAD > files.txt 
          cat files.txt

      - name: Security Check 
        id: security 
        continue-on-error: true 
        run: | 
          while read file do if [[ "$file" == *.py || "$file" == *.c || "$file" == *.cpp ]]; then echo "Analizando $file" python src/ci_cd/security_check.py "$file" fi done < files.txt

      - name: Run Tests
        if: steps.security.outcome == 'success'
        run: |
          pytest

      - name: Telegram Tests Exitosos
        if: steps.security.outcome == 'success'
        run: |
          python - <<EOF
          import requests, os

          requests.post(
              f"https://api.telegram.org/bot{os.environ['TELEGRAM_TOKEN']}/sendMessage",
              json={
                  "chat_id": os.environ["TELEGRAM_CHAT_ID"],
                  "text": "🧪 Pruebas ejecutadas correctamente"
              }
          )
          EOF

      - name: Telegram Codigo Seguro
        if: steps.security.outcome == 'success'
        run: |
          python - <<EOF
          import requests, os

          requests.post(
              f"https://api.telegram.org/bot{os.environ['TELEGRAM_TOKEN']}/sendMessage",
              json={
                  "chat_id": os.environ["TELEGRAM_CHAT_ID"],
                  "text": "✅ Código seguro - Continuando pipeline"
              }
          )
          EOF

      - name: Comment Pull Request
        if: steps.security.outcome == 'failure'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          message: |
            ❌ Vulnerabilidad detectada por IA

            Resultado: VULNERABLE

            El merge fue bloqueado.

      - name: Create Issue
        if: steps.security.outcome == 'failure'
        uses: dacbd/create-issue-action@main
        with:
          token: ${{ github.token }}
          title: Vulnerabilidad detectada
          body: |
            El pipeline detectó una vulnerabilidad.

            Pull Request:
            ${{ github.event.pull_request.html_url }}

      - name: Telegram Vulnerabilidad
        if: steps.security.outcome == 'failure'
        run: |
          python - <<EOF
          import requests, os

          requests.post(
              f"https://api.telegram.org/bot{os.environ['TELEGRAM_TOKEN']}/sendMessage",
              json={
                  "chat_id": os.environ["TELEGRAM_CHAT_ID"],
                  "text": "🚨 Vulnerabilidad detectada. PR bloqueado."
              }
          )
          EOF

      - name: Fail Job
        if: steps.security.outcome == 'failure'
        run: |
          exit 1